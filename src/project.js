import { getTodo, setTodo, getProject, setProject, getMode } from ".";

function projectFunction() {
	let moduleTodo = getTodo();
	let moduleProject = getProject();
}

function displayProject(moduleProject){
	
}